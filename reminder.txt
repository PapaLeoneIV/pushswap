        /***
         * ra() function in stack moves
         -----------------------------------
         * -devi finire di implementare lo swap fra i vari nodi
         * (ricordati di controllare se e' meglio scambiare gli indirizzi)
         * (o semplicemente i valori)
         * -fai la stessa funzione per rb e rr
         * -penso che per faro lo unshift sia molto simile ma solo gestendolo
         * in modo speculare quindi rra e rrb rrr dovrebbero essere veloci da fare
         * 
        */